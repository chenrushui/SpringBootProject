server:
  port: 8000

debug: false

spring:
  redis:
    host: 192.168.140.88
    port: 6379
    database: 1

  #  application-dev.properties
  #  application-test.properties
  #  可以配置多环境结点
  #  --spring.profiles.active=dev
  #  jsckson:
  #    default-property-inclusion: non_null # 全局jackson配置
  #    date-format: yyyy-MM-dd HH:mm:ss #时间戳统一转换
  #    time-zone: GMT+8

  thymeleaf:
    cache: false
  #  rabbitmq:
  #    host: 192.168.140.88
  #    port: 5672
  #    username: guest
  #    password: guest
  #    virtual-host: /

  #  数据库配置信息
  datasource:
    username: pica
    password: Joa5@73&8yAYJ2fe
    # 最后一项表示连接的数据库名字
    # 声明数据库连接的语句中，加入编码的声明
    url: jdbc:mysql://192.168.130.200:3306/pica?characterEncoding=utf-8
    # 指定数据源的类型（对jdbc的封装，有很多种）
    type: com.alibaba.druid.pool.DruidDataSource
    # 配置数据源的详细信息
    # 数据源其他配置(不能绑定到POJO上,POJO上没有提供这些属性)
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    #配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    #filters: stat,wall,log4j
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_null
  application:
    name: spring-boot-micro-8000


mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml
  config-location: classpath:mybatis/mybatisConfig.xml

pagehelper:
  auto-offsetAsPageNum: true
  rowBoundsWithCount:  true
  pageSizeZero:  true
  reasonable: false
  supportMethodsArguments: false
  params:
    pageNum: pageHelperStart
    pageSize: pageHelperRows

#日出的输出级别
logging:
  level:
    root: info

#是否激活swagger
swagger: true


